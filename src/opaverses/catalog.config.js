export const OH_FEATURED_SONG = Object.freeze({
  id: "do-the-panicarena",
  label: "Latest release"
});

export const OH_SONG_SETTINGS = Object.freeze({
  "full-mindness": Object.freeze({ enabled: true }),
  "hallucinating-dum-dum": Object.freeze({ enabled: true }),
  "yeah-lets-do-brunch": Object.freeze({ enabled: true }),
  "splenda-love-rabbit-hell": Object.freeze({ enabled: true }),
  "believe-the-truth-fairy": Object.freeze({ enabled: true }),
  "old-love-story": Object.freeze({ enabled: true }),
  "glittaa-phoenix": Object.freeze({ enabled: true }),
  "not-your-bot-beep-sleep": Object.freeze({ enabled: true }),
  "do-the-panicarena": Object.freeze({ enabled: true }),
  "wellwolf-howl-lehluya": Object.freeze({ enabled: true }),
  "opa-pa-pa-party": Object.freeze({ enabled: true }),
  "optimisms-cute": Object.freeze({ enabled: false })
});